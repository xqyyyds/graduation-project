# 报告系统代码问题综合审阅（2026-04-01）

本文只列问题，不提供解决方案。判断依据以实际源码和最新生成报告为准，不以仓库内历史文档为准。

## 审阅范围

- 后端报告生成链：`Backend/app/services/report.py`
- 后端舆情深读链：`Backend/app/services/opinions.py`、`Backend/app/core/prompts.py`、`Backend/app/core/schemas.py`
- 后端违规审查链：`Backend/app/services/compliance.py`、`Backend/app/agents/nodes.py`
- 后端趋势预测链：`Backend/app/agents/nodes.py`、`Backend/app/agents/quality_gate.py`、`Backend/app/core/prompts.py`、`Backend/app/core/schemas.py`
- 前端报告展示：`frontend/src/views/ReportDetail.vue`、`Backend/app/api/main.py`
- 产物样本：
  - `Backend/output/舆情研判_高校_20260301_1516.md`
  - `Backend/output/舆情研判_社会_20260301_1458.md`

## 总体结论

你指出的问题基本都真实存在，而且大部分不是单个 prompt 写得不够好，而是由以下几层共同造成：

- 输入给 LLM 的素材在关键环节已经被压扁，导致模型没有足够信息做真正的提炼、深读和预测。
- Prompt 和 Schema 带有明显的“模板写作”强约束，天然会把内容推向公文腔、套话化和同质化。
- 工作流中的质量门控只看“有没有生成出来”，几乎不看“是否抓到重点、是否好看、是否一一对应、是否可展示”。
- 违规审查链为了绕过内容安全过滤做了大量脱敏、截断和默认放行，结果牺牲了准确率、颗粒度和可解释性。
- 前端拿到的是一整篇 Markdown 字符串，而不是结构化报告数据，所以展示层只能对长表格做被动修修补补，无法真正做到友好呈现。

## 问题总表

| 用户感受 | 是否存在 | 主要证据 |
| :--- | :---: | :--- |
| 前言每篇都差不多，没抓住重点 | 是 | `Backend/app/core/prompts.py:429-542`、`Backend/app/core/schemas.py:285-315`、`Backend/output/舆情研判_高校_20260301_1516.md:139-160` |
| 重点舆情深读标题无趣、解读不够有意思 | 是 | `Backend/app/services/report.py:596-636`、`Backend/app/services/opinions.py:75-82`、`Backend/output/舆情研判_社会_20260301_1458.md:170-175` |
| 风险预测联网不足、预测点少、内容泛 | 是 | `Backend/app/agents/nodes.py:675-753`、`Backend/app/services/report.py:680-705`、`Backend/output/舆情研判_高校_20260301_1516.md:295-332` |
| 违规审查被 LLM 安全过滤影响严重 | 是 | `Backend/app/services/compliance.py:230-297` |
| 违规结果没有帖子/评论与理由法条一一对应 | 是 | `Backend/app/core/schemas.py:178-219`、`Backend/app/agents/nodes.py:564-594`、`Backend/app/services/report.py:929-991` |
| 前端表格又长又丑，不直观 | 是 | `frontend/src/views/ReportDetail.vue:165-188`、`frontend/src/views/ReportDetail.vue:526-600` |
| 系统架构和工作流设计本身有问题 | 是 | `Backend/app/agents/workflow.py:45-47`、`Backend/app/agents/quality_gate.py:17`、`Backend/app/agents/quality_gate.py:397-417` |

## 一、系统级问题

### 1. 报告系统的核心结构是“粗摘要输入 + 强模板输出 + 弱门控放行”

现象：
当前整套链路不是围绕“抓主轴、抓冲突、抓风险”在组织，而是在围绕“先把内容压成几段摘要，再让模型按固定格式填满”运作。这样做出来的报告，即使语句看起来完整，也很容易丧失重点和锐度。

代码证据：

- `Backend/app/services/report.py:157-160` 把前言输入压缩成 `热点榜单Top15 + 重点深读摘要Top8`。
- `Backend/app/services/report.py:204-217` 对违规和预测的前言输入只保留总数、Top 类别和主题标题。
- `Backend/app/core/prompts.py:463-470` 直接给了前言范文逻辑模板。
- `Backend/app/agents/quality_gate.py:17` 只允许 1 次重试。
- `Backend/app/agents/quality_gate.py:397-417` 重试用尽后直接降级放行。

影响：

- 内容容易“像报告”，但不一定“像有人在认真分析”。
- 生成物通过了流程，不代表通过了阅读体验和表达质量。
- 你感受到的“很死、很像模板、没人愿意看”并不是偶发，而是系统性结果。

### 2. 多条链路之间信息传递严重降维，后面的 Agent 拿不到前面真正有价值的细节

现象：
深读、违规、预测、前言四部分看似都在分析同一批热点，但它们之间传递的并不是细颗粒证据，而是被压缩后的摘要片段。后面的 Agent 因为没拿到足够细节，只能继续泛化。

代码证据：

- `Backend/app/agents/nodes.py:675-691` 给预测链的输入只有 `事件名 + event_overview` 和 `事件名 + overall_risk_level`。
- `Backend/app/services/report.py:204-217` 给前言的违规和预测输入只有总量和标题级摘要。
- `Backend/app/agents/nodes.py:566-579` 在 state 中保存的违规评论原文只保留了 `index/content/category/risk_level`，没有逐条理由、逐条条款、逐条处置建议。

影响：

- 前言抓不到本期舆情真正的主矛盾。
- 预测拿不到足够丰富的现实线索。
- 违规附录无法做到逐条精确解释。

### 3. 工作流里存在“概念上完整、实际上断裂”的设计

现象：
代码里保留了很多“看起来更完整”的设计痕迹，但实际生效链路并没有真正使用它们，导致维护认知和真实行为存在偏差。

代码证据：

- `Backend/app/agents/workflow.py:12-13` 历史节点被注释掉，`agent_historical_node` 未启用。
- `Backend/app/agents/workflow.py:76-79` 当前工作流直接从 BC 质量门控进入 D，并没有历史回顾节点参与预测。
- `Backend/app/agents/nodes.py:19` 仍导入 `agent_forecast`，但实际 D 节点走的是 `ReAct Agent` 路径。

影响：

- 预测链的“历史规律支撑”在架构上并不稳定。
- 阅读代码时容易误以为系统有完整的历史回顾支撑，但实际生成链不是这样。
- 系统设计和实际产物之间存在认知错位。

## 二、前言部分的问题

### 4. 前言输入素材过于粗糙，无法支撑“总结出真正重点”

现象：
前言要承担“定调、总括、提主轴、承接风险”的任务，但实际拿到的输入非常粗。模型只能围绕“热度高、情绪强、议题多、信息不透明”这些泛词去写。

代码证据：

- `Backend/app/services/report.py:134-160` 前言的事件素材是榜单和少量概述拼接，没有单独抽取主线冲突、情绪结构、治理压力。
- `Backend/app/services/report.py:204-210` 违规输入只写“共发现多少条、主要集中在哪些类别、典型理由一句话”。
- `Backend/app/services/report.py:213-217` 趋势输入只保留主题标题，没有保留背景、概率、证据依据。

产物证据：

- `Backend/output/舆情研判_高校_20260301_1516.md:139-160` 的前言是标准五段式，但没有一句真正概括本期高校舆情的主轴。
- 这段文字把“国家线、查分、考公、四六级”并列铺开，却没有先判断它们是否属于同一压力链条。

影响：

- 前言看起来完整，实际上没有完成“总编定调”的任务。
- 读者看完后仍不知道“本期最值得警惕的到底是什么”。

### 5. Prompt 和 Schema 强制前言走固定套板，直接制造同质化

现象：
前言不是自由总结，而是被要求按严格的五段式、三特征、公文型结语去写。即使输入不同，输出也会高度相似。

代码证据：

- `Backend/app/core/prompts.py:463-470` 明确给出“总体来看…其一、其二、其三…”的范文逻辑。
- `Backend/app/core/prompts.py:491-531` 强制要求 3 个特征、每个不少于 5 句话、结语至少 3 条治理建议。
- `Backend/app/core/schemas.py:297-315` 继续从 Schema 层硬编码“3 个特征”“不少于若干句”“严肃风格”。
- `Backend/app/services/report.py:523-530` 渲染时再次把特征强制加上“其一、其二、其三”。

产物证据：

- `Backend/output/舆情研判_高校_20260301_1516.md:143-149` 和 `Backend/output/舆情研判_社会_20260301_1458.md:143-149` 的特征段落结构几乎是同模版换词。

影响：

- 不同领域的前言会被写成同一种口气。
- 系统会持续生成“像合格范文”的内容，而不是“像抓住重点的编辑导语”。

### 6. 前言结尾天然滑向空泛治理话术

现象：
当前前言结尾被要求给出宏观治理建议，且风格强制严肃，导致结尾很容易落入“加强监测、完善机制、及时回应、加强沟通”这种标准公文语。

代码证据：

- `Backend/app/core/prompts.py:522-531` 结语要求至少 3 条宏观治理建议。
- `Backend/app/core/schemas.py:312-315` 结语字段描述继续强化同类表达方式。

产物证据：

- `Backend/output/舆情研判_高校_20260301_1516.md:160`
- `Backend/output/舆情研判_社会_20260301_1458.md:160`

影响：

- 结语几乎必然“官方、平、泛”。
- 读者最后看到的不是一句有记忆点的判断，而是一段通用建议。

## 三、重点舆情深读的问题

### 7. 深读标题没有编辑层加工，直接使用热搜原题，天生无趣

现象：
重点舆情深读最适合做成“带钩子的标题 + 有立场的解读”，但当前标题层几乎没有单独生成逻辑，直接把事件名塞进章节标题。

代码证据：

- `Backend/app/services/report.py:609-616` 直接取 `raw_title/event_name/topic` 作为标题。
- 全仓库检索 `raw_title` 仅命中 `Backend/app/services/report.py:611-613`，上游没有稳定赋值，这个“优先使用原始热搜标题”的分支实际上是死的。

产物证据：

- 样本报告中的标题是 `《国家线 砍一刀》`、`《一点点》`、`《伊朗首都发生爆炸》` 这类原始热搜词，不具备深读标题应有的表达力。

影响：

- 章节标题没有抓人能力。
- 深读板块的第一印象已经输掉，后面的内容再好也很难带起阅读欲望。

### 8. 深读联网搜索非常浅，只做了低深度通用检索

现象：
深读并没有围绕事件类型做差异化联网，而是统一拼一个很通用的检索词，并且只拿很少的结果。

代码证据：

- `Backend/app/services/opinions.py:79-82` 搜索词固定为 `事件名 + 周期提示 + 事件详情 官方通报`。
- `Backend/app/services/opinions.py:82` 只取 `max_results=3`，`search_depth="basic"`。

影响：

- 搜索结果更像“补一个背景”而不是“建立完整上下文”。
- 复杂事件很难被真正搜深，尤其是需要多角度事实、后续发酵、争议节点的事件。

### 9. 深读生成提示词高度“智库化”，强烈推向公文腔

现象：
深读想做得有意思，至少需要给模型留出“编辑感、观察感、可读性”的空间。但现在的 Prompt 和 Schema 把它钉死在“国家级智库、社论评论、硬性篇幅、严肃风格”上。

代码证据：

- `Backend/app/core/prompts.py:90-96` 角色被定义为“国家级智库的首席社会学专家”“严格模仿高维认知结构输出”。
- `Backend/app/core/prompts.py:124-160` 多处硬性长度和固定思维链。
- `Backend/app/core/schemas.py:112-123` 对概述、观点、深评都要求严肃书面语和固定结构。

影响：

- 语言很容易变得“工整但无趣”。
- 深读更像考试答题或机关材料，不像有人真正读完舆情后写出的深刻评论。

### 10. 深读选题只是取前 5 个去重事件，不看“可分析性”和“可读性”

现象：
当前核心事件的深读入选逻辑主要看前序排序和去重，不看事件是否适合做深挖，不看事件是否具备标题可塑性、观点冲突度、受众兴趣度。

代码证据：

- `Backend/app/agents/nodes.py:333-349` 在 `all_events` 上串行遍历，满足条件后直接分析，达到 `ANALYZE_EVENT_COUNT` 就停止。

影响：

- 入选的 5 个核心事件未必是最适合做“深读”的 5 个。
- 有的事件热度高，但内容单薄；有的事件标题短，但无法承载好看解读。

## 四、未来趋势与战略预警的问题

### 11. 预测链拿到的输入太薄，无法形成真正有证据感的未来推演

现象：
预测不是在基于“详细当前态势 + 违规颗粒 + 历史规律 + 未来事件”做推演，而是在基于很粗的当前概述和风险等级做泛化判断。

代码证据：

- `Backend/app/agents/nodes.py:675-683` 当前舆论输入只有 `event_name + event_overview`。
- `Backend/app/agents/nodes.py:685-691` 违规输入只有 `event_name + overall_risk_level`。
- `Backend/app/agents/workflow.py:12-13` 历史节点未启用。

影响：

- 预测容易变成“把当前热点延长一个月再说一遍”。
- 缺少真正的触发条件、生活场景、群体差异和连锁反应。

### 12. 预测 Prompt 看上去要求很多，但实际展示层把最重要的信息都丢了

现象：
Schema 和 Prompt 里明明要求每个点有概率、证据依据、背景、3-5 个点，但最终报告页面没有把这些关键要素完整展示出来。

代码证据：

- `Backend/app/core/schemas.py:227-277` `ForecastPoint` 和 `TrendForecastReport` 定义了 `likelihood`、`evidence_basis`、`evidence_sources`、每个 topic 下 `3-5` 个 points。
- `Backend/app/services/report.py:688-705` 实际渲染时只输出 `topic_name`、`background`、`subtitle`、`content`。
- `Backend/app/services/report.py:697-704` 渲染层完全没有输出 `likelihood`、`evidence_basis`。
- `Backend/app/services/report.py:682-686` 也没有输出 `evidence_sources`。

影响：

- 即使模型真搜到了东西，读者也看不到推演依据。
- 预测看起来像空口判断，而不是基于信息支撑的预警。

### 13. 质量门控没有真正卡住“每个议题下点数不足”的问题

现象：
Schema 要求每个 topic 下有 3-5 个 points，但质量门控并没有对这个关键要求做强检查，所以最终报告里经常出现“每个议题只有 1 个点”的情况。

代码证据：

- `Backend/app/core/schemas.py:258-260` 明确写了每个 topic 下的 points 必须 `3-5个`。
- `Backend/app/agents/quality_gate.py:59-66` 对 D 的完整性检查只要求“是否有 3-5 个预测主题”，没有要求“每个主题是否有 3-5 个 points”。

产物证据：

- `Backend/output/舆情研判_高校_20260301_1516.md:299-332` 每个主题实际上只有一个 `####` 风险点。
- `Backend/output/舆情研判_社会_20260301_1458.md:299-332` 同样是每个主题只展开一个点。

影响：

- 输出直接低于 Schema 设定的表达密度。
- 预测看起来像 5 条短评，不像真正的风险推演。

### 14. 预测语言正式有余，场景感和锐度不足

现象：
你提到“语句不有趣、很公文、观众不喜欢看”，这个判断和代码设定高度一致。当前预测链从角色、风格到标题格式都明显在往正式公文化方向压。

代码证据：

- `Backend/app/core/prompts.py:329-332` 定义为“国家级智库的首席战略预警专家”。
- `Backend/app/core/prompts.py:395-412` 强制明确判断、正交议题、统一小标题格式。
- `Backend/app/core/schemas.py:250-256` `topic_name/background` 的风格说明偏严肃公文。

产物证据：

- `Backend/output/舆情研判_高校_20260301_1516.md:303-332` 语言高度类似“情况判断 + 建议关注”。
- `Backend/output/舆情研判_社会_20260301_1458.md:303-332` 也是典型“建议部门加强…”式结尾。

影响：

- 预测部分很难形成阅读记忆点。
- 即便内容勉强合格，也不具备传播层面的吸引力。

## 五、违规判定链的问题

### 15. 为了绕过内容安全过滤，系统主动修改审查输入，削弱了逐字逐句判定能力

现象：
违规审查最需要原文精度，但当前实现为了避免 provider 的安全拦截，对原文做了大量掩码、替换、截断。

代码证据：

- `Backend/app/services/compliance.py:108-200` `_sanitize_for_llm` 会遮盖敏感词、替换敏感短语、截断长文本。
- 其中 `强奸幼女|性侵未成年` 会被整体替换为 `[涉未成年人侵害案]`，很多极关键语义被抽象化。

影响：

- 审查模型看到的不是原文，而是被改写过的版本。
- 这会直接影响类别判断、理由对齐和条款贴合度。

### 16. 内容安全过滤一旦触发，系统会走“高度脱敏 + 只看前 10 条评论 + 默认合规”的降级路径

现象：
这不是“尽力保留审查能力”的降级，而是在很多难样本上直接放弃深审。

代码证据：

- `Backend/app/services/compliance.py:262-286` 过滤触发后只保留前 10 条评论再试一次。
- `Backend/app/services/compliance.py:287-292` 第二次失败后直接返回 `is_post_violated=False, violated_comments=[]`。
- `Backend/app/services/compliance.py:294-297` 其他异常同样返回默认安全。

影响：

- 会出现明显的假阴性。
- 越是敏感、越需要精准审查的内容，越可能被系统当成“安全”放过。

### 17. 代码里存在一个与内容过滤相关的死分支，说明“过滤拦截状态”没有被正确传递

现象：
审查链后续逻辑试图识别 `_content_filter_blocked`，但前面并没有把这个标记真正写出来。

代码证据：

- `Backend/app/services/compliance.py:328-335` 在 `batch_audit_with_rag` 中检查 `getattr(batch_res, "_content_filter_blocked", False)`。
- `Backend/app/services/compliance.py:230-297` 的 `batch_audit` 从头到尾没有设置 `_content_filter_blocked`。

影响：

- 与过滤状态相关的后续分支并不可靠。
- 系统以为自己处理了“内容过滤场景”，实际上标记链条是不闭合的。

### 18. 违规数据模型天然不支持“每条评论/帖子一一对应理由、法条、处置”

现象：
你指出“前面一个总体风险，后面一个总体违规理由，根本没一一对应”，这在当前 Schema 设计下几乎是必然发生的。

代码证据：

- `Backend/app/core/schemas.py:178-186` `BatchComplianceResult` 只有 `is_post_violated` 和 `violated_comments`。
- `Backend/app/core/schemas.py:203-219` `ComplianceEvidenceReport` 是“对一条帖子（含评论列表）的整体证据链”，不是对每个违规项单独给证据链。

影响：

- 从数据结构上讲，系统没有为“逐条评论单独理由/法条/处置”预留稳定位置。
- 后端最终只能把很多违规项揉成一个整体判决书。

### 19. Node C 在状态里保存的违规结果已经丢失了逐条解释信息

现象：
即使前面的模型有逐条违规项，到了状态存储阶段也只保留了很薄的一层信息。

代码证据：

- `Backend/app/agents/nodes.py:566-579` 保存到 `violated_comment_originals` 的字段只有 `index/content/category/risk_level`。
- 这里没有逐条 `reasoning`、逐条 `matched_laws`、逐条 `disposal_suggestion`。

影响：

- 报告生成器到了最后阶段已经没有足够信息做一一对应展示。
- 前端也无从做逐条卡片或逐条明细。

### 20. 报告附录把主贴和多条违规评论合并到一个表格单元格，再配一个总体理由和总体法条

现象：
这是你指出的最核心产品问题之一，而且源码里是明确这样实现的，不是偶然渲染错位。

代码证据：

- `Backend/app/services/report.py:929-958` 把主贴和所有违规评论拼成一个 `violation_cell`。
- `Backend/app/services/report.py:921-924` 风险等级使用整体 `overall_risk_level`。
- `Backend/app/services/report.py:960-981` 判定理由和法条来自整体 `evidence_report` 和整体 `matched_laws`。
- `Backend/app/services/report.py:983-991` 整个表格行只对应一个建议。

产物证据：

- `Backend/output/舆情研判_社会_20260301_1458.md:410-418` 同一行里塞入多条评论，只配一段总理由和总法条。
- `Backend/output/舆情研判_社会_20260301_1458.md:449-453` 同一行里多条评论被统一归到一组条款和一条建议。

影响：

- 读者无法判断“哪一句话触发了哪一条规则”。
- 判定可解释性非常差。
- 表格视觉上也必然会变得又长又乱。

### 21. 违规分类和法条贴合度在样本报告中已经出现明显漂移

现象：
样本里已经能看到一些判定结果更像“总体感受性定性”，而不是紧贴原句的一一分类。

产物证据：

- `Backend/output/舆情研判_社会_20260301_1458.md:465` “太恶心了”被判为高风险网暴相关内容，法条贴合度偏弱。
- `Backend/output/舆情研判_社会_20260301_1458.md:449-450` 围绕继父事件的部分评论被打到色情/感官不适/侮辱等不同条款，边界呈现混杂。
- `Backend/output/舆情研判_社会_20260301_1458.md:453` 多条不同性质的评论被整体判到“未成年人保护法”这一条，解释粒度不够。

代码侧原因证据：

- `Backend/app/services/compliance.py:347-453` RAG 检索和证据链生成都是围绕“整组违规项”做，而不是对每条项分别建模。

影响：

- 最终表格里会出现“看上去像在判，但没有真正判准”的感觉。
- 用户对系统专业性的信任会被明显削弱。

### 22. 合规质量门控没有检查“一一对应展示”和“过滤造成的漏检”

现象：
当前 C 的质量门控主要看是否审查了所有内容、是否有 quote、是否有法规依据，但并不检查逐条映射质量，也不检查因内容过滤导致的大面积漏检。

代码证据：

- `Backend/app/agents/quality_gate.py:51-58` C 的评估标准只覆盖覆盖度、准确性、是否有法规依据。
- `Backend/app/agents/quality_gate.py:274-291` C 的摘要也只摘要类别和是否有法规，不摘要逐条理由映射。

影响：

- 即使生成出“合规表格很难看、很多项对应不上”的结果，质量门控也可能给过。

## 六、前端展示的问题

### 23. 报告文件把整段 CSS 写进 Markdown，导致产物与消费端互相污染

现象：
当前 Markdown 报告不是纯内容文档，而是把样式直接写进了文件头部。前端又不得不在渲染前把它剥掉。

代码证据：

- `Backend/app/services/report.py:375` 开始构造 `<style>`。
- 最新报告 `Backend/output/舆情研判_高校_20260301_1516.md:1-129` 开头就是完整 CSS。
- `frontend/src/views/ReportDetail.vue:104-107` 渲染前专门用正则删除 `<style>`。

影响：

- 产物本身不干净。
- 标题提取、下载、二次利用都会受影响。
- 前后端形成了一种脆弱的“你先污染，我再清洗”的耦合关系。

### 24. 报告列表标题提取机制会被 `<style>` 头污染，容易退化成文件名

现象：
后端报告列表 API 通过读取 Markdown 第一行来取标题，但当前第一行通常是 `<style>`，因此标题提取机制经常失效。

代码证据：

- `Backend/app/api/main.py:501-509` 只读取第一行，只有第一行以 `#` 开头才认作标题。
- 最新报告第一行是 `<style>`，见 `Backend/output/舆情研判_高校_20260301_1516.md:1`。

影响：

- 报告列表页标题容易显示成文件名，而不是人类可读标题。
- 报告管理体验和检索体验都会变差。

### 25. 前端只能把长表格截断后弹窗查看，本质上是在对结构问题做被动补丁

现象：
当前报告详情页没有真正理解报告结构，而是把整篇 Markdown 渲染出来，再对超长单元格打补丁。

代码证据：

- `frontend/src/views/ReportDetail.vue:165-188` 遍历所有表格单元格，超过 60 字就标记为 `truncated-cell`。
- `frontend/src/views/ReportDetail.vue:156-162` 点击单元格后弹一个“完整内容”对话框。
- `frontend/src/views/ReportDetail.vue:526-600` 通过一组硬编码列宽样式强行压 6 列大表。

影响：

- 交互层是在应付超长表格，而不是在解决信息展示问题。
- 当违规内容本身就是多帖多评聚合时，弹窗也只是把混乱内容放大。

### 26. 报告页是“一整篇长 Markdown”，没有按内容复杂度拆解成适合阅读的结构

现象：
报告详情页当前更像一个 Markdown 阅读器，而不是一个报告产品页。

代码证据：

- `frontend/src/views/ReportDetail.vue:45-50` 直接用 `v-html` 挂整篇渲染后的 HTML。
- `Backend/app/api/main.py:424-433` 获取报告内容时直接返回整篇 Markdown 字符串。

影响：

- 重点舆情、预测、违规明细全部摊在一个长滚动页面里。
- 一旦附录很长，整体阅读体验会迅速恶化。
- 想做更直观的分层展示时，前端缺乏结构化数据抓手。

## 七、数据与产物一致性问题

### 27. 报告列表、报告内容、仪表盘统计使用的是不同数据源和不同抽取方式

现象：
系统当前不是围绕一个统一的“报告对象”在工作，而是文件系统、Markdown 字符串、Mongo 中的会话记录各取各的。

代码证据：

- `Backend/app/api/main.py:462-523` 报告列表来自 `output/*.md`。
- `Backend/app/api/main.py:424-433` 报告详情直接返回 Markdown 文件内容。
- `Backend/app/agents/nodes.py:815-827` 另有 `report_sessions` 写入 Mongo。
- `Backend/app/db/mongo_manager.py:412-442` 仪表盘违规统计读取的是最新一条 `report_sessions`。

影响：

- 列表、详情、统计三者之间不是同一个结构化真相源。
- 某些页面显示的是文件标题，某些页面显示的是解析标题，某些统计取的是最新 Mongo 会话。

### 28. 仪表盘违规统计函数名写着“聚合全局”，实际只读最新报告

现象：
代码注释和函数行为不一致，容易误导维护者对系统统计逻辑的理解。

代码证据：

- `Backend/app/db/mongo_manager.py:410` 注释写的是“从 report_sessions 聚合全局违规统计”。
- `Backend/app/db/mongo_manager.py:414-421` 实际只读取最新一条报告。

影响：

- 数据语义和界面认知容易错位。
- 维护者很容易误以为仪表盘是全局趋势统计，实际上不是。

## 八、产物层面的直接表现

### 29. 最新高校报告已经直接体现出“前言模板化、预测单薄化、附录聚合化”

样本证据：

- `Backend/output/舆情研判_高校_20260301_1516.md:139-160`
  - 前言为固定结构。
  - 特征段落是标准“其一、其二、其三”。
  - 结语是通用治理话术。
- `Backend/output/舆情研判_高校_20260301_1516.md:295-332`
  - 预测部分 5 个主题各只有 1 个点。
  - 每个点都是“背景导语 + 一段判断 + 建议关注”。
- `Backend/output/舆情研判_高校_20260301_1516.md:337-390`
  - 附录从结构上已经进入长表格模式。

### 30. 最新社会报告已经直接体现出“违规一锅炖、理由法条不逐条贴合、表格极长”

样本证据：

- `Backend/output/舆情研判_社会_20260301_1458.md:406-418`
  - 单行聚合多条评论。
  - 一个总风险等级覆盖多条评论。
  - 一个总理由覆盖多条评论。
- `Backend/output/舆情研判_社会_20260301_1458.md:439-453`
  - 未成年人相关事件下，多种表达混合在一起展示。
  - 条款和建议仍是整体性的，不是逐条对应。
- `Backend/output/舆情研判_社会_20260301_1458.md:461-469`
  - 个别简短评论的定性与法条看起来已经出现颗粒度不匹配。

## 结论性判断

从实际代码和最新报告样本看，你提出的核心批评都成立，而且问题主要集中在以下几个根因层面：

- 报告写作不是“围绕重点组织信息”，而是“围绕模板组织信息”。
- 违规审查不是“围绕逐条证据组织判定”，而是“围绕整组结果组织判决书”。
- 预测不是“围绕可验证证据组织推演”，而是“围绕当前摘要延长表达”。
- 前端不是“围绕结构化报告展示内容”，而是“围绕大 Markdown 和长表格做补丁式渲染”。

因此，当前问题不是某一段 prompt 用词不够灵，而是设计架构、Schema 粒度、节点输入、质量门控和前端承载方式一起出了方向性偏差。

本文到此为止，不含解决方案。
