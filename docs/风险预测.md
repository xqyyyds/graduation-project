但现在还有 4 个问题没完全解决
问题一：你这版 prompt 已经好很多，但schema 还必须同步

如果 schema 还是旧的那套：

trigger

spread_path

offline_scene

online_scene

那这版 prompt 的很多新要求会被旧 schema 拖回去。

你这版 prompt 里已经改成了：

audience

scene

evolution_path

content

summary_paragraph

这说明你的 schema 也必须跟着改。
不然模型会一边被 prompt 告知按新逻辑写，一边被 schema 拉回旧字段，最后输出就会拧巴。

也就是说

如果你只是改了模板，没改 Pydantic schema，效果不会完全出来。

问题二：summary_paragraph 还是可能“机械三拍”

这是下一层问题。

你现在规定三拍式很好，但模型可能写成：

第一句：某场景出现

第二句：讨论扩大

第三句：上升为更大议题

这样虽然合规，但会显得很公式。

怎么改

你要补一句：

三拍式是内部节奏要求，不是显性模板。三句之间必须自然衔接，不能写成“先发生A、随后变成B、最后升级为C”的机械履约句。


否则它会“完成任务”，但不一定“写得好”。

问题三：还缺一个“坏段落黑名单”

你已经禁了：

值得关注

可能引发

舆情发酵

但还不够。

因为预测段最常见的坏写法是：

某风险需要警惕

某问题可能放大

某情绪持续升温

这些都没信息密度。

建议你直接再加一条：

禁止用以下句式充当 summary_paragraph 的骨架：
- “某类风险需要重点关注”
- “相关讨论可能进一步升温”
- “舆情可能由此发酵扩散”
- “该问题或将引发更大范围关注”
- “有关方面应提前防范”
如果一段话删掉这些句子后几乎没有信息增量，说明该段不合格。


这个很有用。

问题四：还缺“不要把判断写得太像内部材料”

你现在 prompt 还是偏“专业”，但还没完全阻止“报告腔”。

建议加一句：

读者看到的 summary_paragraph 应该像一段成熟专栏中的预判，而不是会议纪要、专项提示、工作通报或应急预案摘要。


这句简单，但很有效。

我建议你现在再补 3 刀，就更稳了
第一刀：补一条“summary_paragraph 不是字段转述，也不是合规检查答案”

直接加在第 5 条后面：

- `summary_paragraph` 不是对 `audience / scene / evolution_path / content / evidence_basis` 的逐项转述，也不是对规则的答题式作答；它必须像一段自然写出来的预测文字。

第二刀：加“首句禁止抽象判断”

非常重要。

- `summary_paragraph` 的首句禁止以“当前/未来/这一风险/这一议题/值得关注/需要警惕/可能出现”等抽象判断开头，必须从具体场景、截图、回应、视频、现场瞬间或现实动作切入。


这会立刻改善第一句质量。

第三刀：加“结尾不能只是上价值”

结尾特别容易烂尾，写成：

最终可能引发更大关注

值得进一步警惕

需要高度重视

这种没意思。

建议加：

- `summary_paragraph` 结尾必须明确指出将升级成哪类更大的公共议题，例如规则公平、责任分配、平台治理、机构公信力、群体对立、消费信任等；不得只以“值得警惕/需重视”收尾。

我帮你再精修一下你现在这版最关键的一段

你现在第 5 条是：

第一句落在起火场景。
中间写清争议如何被推大。
结尾落到真正值得警惕的升级方向。

我建议替换成下面这版，更强：

**5. summary_paragraph 三拍式（硬规则）**
- 首句必须从一个具体、可传播的起火场景切入，禁止从抽象判断开头。
- 中段必须写清讨论如何从眼前问题滑向更大争议，句子之间要自然推进，不能机械翻译字段。
- 结尾必须点明最终会升级成哪类更大的公共议题，不能只写“值得关注/需要警惕/可能发酵”。
- `summary_paragraph` 不是对内部字段的逐项转述，也不是答题式履约；它必须像一段自然写出的预判文字。


这版更稳。

现在这版如果配合正确 schema，效果会到什么水平
如果 prompt 改了，schema 也改成：

audience

scene

evolution_path

content

summary_paragraph

那我判断：

summary_paragraph 能达到：

大多数时候：自然、成段、可读

一部分时候：有判断、有画面、挺像专栏

少数时候：会略公式化，但已经不太像台账

这已经比之前强不少。

但还是那句话：不能迷信 prompt 一步到位

你这版已经很接近“该写的都写了”，所以接下来更关键的不是再加 20 条规则，而是：

1. 确保 schema 同步

这是第一位。

2. 确保输入真的有“未来节点”和“现实场景”

如果输入太空，模型再会写也会空转。

3. 看几轮真实样本，再针对“坏段落模式”补黑名单

因为最终坏味道往往是固定的。

最后直接回答你这句“现在呢”

现在这版已经明显更好了，可以生成更像读者会愿意看的预测段落，但前提是 schema 也要同步改掉旧字段。

如果 schema 还是旧的，效果只能释放一半。
如果 schema 也同步，这版已经进入“能用且有质感”的区间了。

你把新版 ForecastPoint / ForecastTopic / TrendForecastReport 也贴出来，我可以直接帮你做最后一轮对齐，看 prompt 和 schema 有没有打架。