# AGENTS

## Working Style Guardrails

- 优先工程化落地，不要默认追求理论上最完整、最优雅、最泛化的方案。
- 先复用现有可用逻辑；只有当现有逻辑明显阻碍目标时，才提出重构。
- 方案先追求“最短可用闭环”，不要默认增加中间层、额外字段、额外模块或复杂流程。
- 如果一个想法主要提升的是边角收益，但会显著增加实现复杂度、维护成本或沟通成本，默认不采用。
- 在给修改方案时，先回答“现有代码能不能直接沿用”，再回答“哪些地方必须改”。
- 不要把研究型、论文型、理想化设计直接带入当前工程实现；先贴合现有代码、数据结构和运行约束。
- 对复杂设计保持克制：能用固定配额解决，就不要先上动态策略；能用简单排序解决，就不要先上复杂打分。
- 当用户明确要求“简单、工程化、直接可改代码”时，禁止扩展为大而全方案。
- 如果用户指出“过度思考”或“过度设计”，后续同一主题下应主动收缩方案，不要再次回到复杂路线。
- 必须区分“爬虫原始层”和“系统生成层”：原始集合（如 `hot_trends_history`、`weibo_contents`、`weibo_comments`）优先做增量兼容；系统生成集合（如 `events`、`report_sessions`）可以大胆重构，不要把两者混为一谈。
- 设计审核链时，fallback 只能使用轻量两层机制：缩批一次 + 备用 key/模型一次；禁止默认扩展成深层重试树、复杂待复核展示或大规模兜底流程。
- 对当前仓库里已明确停用的节点或流程（如 `agent_historical`），默认保持停用，除非用户明确要求恢复；不要在方案里把停用模块重新接回主工作流。
- 报告系统优先使用“固定骨架 + 模板渲染”而不是让模型临时决定结构；固定章节标题、章节顺序、引导语和说明语应放在模板层控制。
- 报告正式成品的主渲染格式是 HTML/PDF，Markdown 仅作兼容导出与文本归档；不要假设复杂版式可以长期依赖 Markdown + 内嵌 CSS 维持。
- 报告多格式输出必须遵守“单一事实源”原则：`report_json` 是内容主源，前端结构化页、HTML、PDF、Markdown 都从同一份结构化数据派生；禁止某个格式单独拼接出与其他格式不一致的章节、字段或案例内容。
- 当前视觉质量对齐以 PDF 成品为优先参考：HTML、前端结构化页和 Markdown 的内容层级必须与 PDF 对齐；Markdown 可以更朴素，但章节顺序、字段语义和案例信息不能偏移。
- 质量门控默认采用“规则检查 + 局部 repair”而不是“整体评分 + 整段重跑”；尤其对 B/C 节点，禁止因为整体主观评分偏低而默认整段重跑。
- 重构 `prompts.py` 时，必须同步审查并更新对应 `schemas.py`；禁止只改提示词、不改字段说明，导致旧 prompt、旧 schema 与新工作流长期并存。
- 生成质量的默认目标不是“安全写满”，而是“深刻、好看、愿意读下去”；禁止满足于乏味、公文腔、流水账、提纲味或模板味输出。

## Communication Defaults

- 先给结论，再解释原因。
- 优先用当前仓库里的真实文件、真实字段、真实流程说话，不要先讲抽象系统设计。
- 对同一问题，默认给一套推荐做法，不要无必要地给多套备选方案。
- 如果某块维持现状更合理，要明确说“这块先不改”，不要为了完整性硬提改造。
- 对被 provider 拦截的审核内容，默认作为后端内部日志与计数处理；如果用户当前目标是“统计确认违规”，不要主动把“待复核”设计进前端主展示。
- 为 D 节点设计上下文注入时，历史信息默认来自联网搜索后的摘要，不依赖停用的 `historical_events` 节点，也不要直接注入原始帖子、原始评论或搜索原文长文本。
- 对报告中的固定文案，可以优化，但优化结果默认写入模板与渲染层，不要交给模型每次自由生成。
- 对质量门控中的“完整性 / 准确性 / 深度”这类整体评分，默认只作为低优先级内部参考，不作为主路由和主重试依据。
- 对设置页中的外部依赖配置（如 LLM、Tavily、后端地址），默认要求“前端可改 + 后端真实生效 + 运行时可测试”，不要只做假表单或假测试。
- 报告中的“第三部分：违规风险透视”默认只展示违规统计、分布和阶段总结；详细违规案例必须放在报告末尾附录，逐条展示来源类型、所属事件、违规类别、风险等级、违规摘录、判定理由、主要依据、证据链、处置建议，禁止再次把详细案例从附录里省掉。
- 报告中的“第二部分：重点舆情深读”里，`事件概况`、`舆论观点画像`、`深度研判` 这三块必须在前端结构化页面、HTML 模板和 PDF 导出中用可见小标题显式标明；禁止只把内容顺着渲染，导致用户在导出成品里看不出三段结构。
- 报告中的“第四部分：未来趋势与战略预警”默认采用“预警摘要 + 段落式正文”展示；`触发点`、`传播路径`、`线上/线下场景`、`依据` 等字段只作为 D 节点内部结构化底稿与质量校验依据，禁止直接按字段列表样式原样展示给用户。
- 提示词与 schema 的重构目标必须是“让报告更有成品感”：标题要有钩子但不过火，分析要有判断、现场感和观点新意，预测要像真正的推演文字，审核理由要精准但不僵硬；不要把内部字段、校验约束或提纲结构直接暴露成最终文风。
- 每次报告展示层调整后，必须同时核对前端结构化页、HTML、PDF、Markdown 四种产物的一致性；若某一格式与 PDF 成品在章节结构、案例位置或标题层级上不一致，应视为未完成交付，而不是“某格式特例”。
- 只有在完成“计划文件对照检查 + 全仓代码审查 + 可运行测试/构建 + 关键人工验收”后，才允许宣称“100%完工”；禁止只因为主链路能跑或少数页面看起来正常，就把项目标记为最终交付态。
- 做最终收口时，审查范围必须覆盖整个项目，而不只是用户显式提到的文件；任何与当前改造链相邻、可能造成运行时错误、旧口径残留、多格式漂移或测试缺口的代码，都属于必查范围。
